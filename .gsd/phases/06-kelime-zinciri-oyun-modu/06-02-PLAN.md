# S02: Zincir UI ve Skor Kaliciligi

**Goal:** Zincir modu UI tamamlanacak, ana ekran butondan zincir başlatılabilecek, oyun sonu skor kalıcı olacak, CSS ve i18n eksikleri giderilecek.
**Demo:** Zincir modu açılır, oynanır, en iyi skor kaydedilir.

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Zincir UI eksikleri giderildi: home.js'de btn-chain i18n render eklendi, testte grafem hatası düzeltildi. _(20m)_
  chain.js ekranındaki eksik parçaları tamamla:
  1. openChain çağrısında ensurePool + startChain çalışıyor, UI'da input'a doğru odaklanma var.
  2. _refresh fonksiyonu çalışıyor mu kontrol et (input güncellenmeyebilir).
  3. _showEnd ekranını S.stats'tan okuyor — doğrulama yap.
  4. HTML'de zincir butonu stilinin diğer butonlarla uyumlu olduğundan emin ol.
  5. chain.config ayarlarını doğru kullan (poolFromInfo).
  - Files: `js/screens/chain.js`, `index.html`, `js/data/config.js`
  - Verify: npm test
- [x] **T02**: Zincir CSS ve build dogrulamasi basarili - var olan CSS yeterli, build sorunsuz _(15m)_
  Zincir panelinin görünümünü düzenle:
  1. .chain-status, .chain-input, .chain-warn, .chain-lat class'larının tanımlandığından emin ol (components.css'te veya ekle)
  2. Giriş kutusunun mobilde düzgün göründüğünü kontrol et
  3. `Zincir` butonunun diğer ana ekran butonlarıyla uyumlu olduğunu doğrula
  - Files: `css/components.css`
  - Verify: npm run build
- [x] **T03**: Tum testler, lint ve build basarili - zincir modu kullanima hazir _(15m)_
  1. Zincir UI için basit bir test ekle (screens/chain.js'in openChain fonksiyonunun panel açtığını doğrula)
  2. Testlerin tamamını çalıştır: npm test
  3. Build'i çalıştır: npm run build (Vite build)
  4. Varsa ESLint hatalarını düzelt
  - Files: `js/__tests__/chain.test.js`, `js/screens/chain.js`
  - Verify: npm test && npm run build
</tasks>

## Files Likely Touched

- js/screens/chain.js
- index.html
- js/data/config.js
- css/components.css
- js/__tests__/chain.test.js
