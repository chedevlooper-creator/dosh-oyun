# S01: Workbox Runtime Caching ve Pack Precache

**Goal:** Workbox runtimeCaching stratejisi ekle, pack JSON'ları için StaleWhileRevalidate kullan.
**Demo:** İnternet kapalıyken pack değiştirilebilir; tüm pack'ler ilk yüklemede cache'lenir.

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Workbox runtimeCaching eklendi: StaleWhileRevalidate (pack'ler), CacheFirst (asset'ler), NetworkFirst (HTML). _(25m)_
  1. vite.config.js workbox bloguna runtimeCaching ekle
  2. Pack JSON'ları için StaleWhileRevalidate stratejisi
  3. Diğer asset'ler için CacheFirst (js/css/font) ve NetworkFirst (html)
  4. globPatterns'e pack JSON'larını ekle (precache)
  5. npm run build ile doğrula
  6. npm test ile doğrula
  - Files: `vite.config.js`
  - Verify: npm run verify
</tasks>

## Files Likely Touched

- vite.config.js
